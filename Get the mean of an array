function getAverage(marks){
  //TODO : calculate the downwar rounded average of the marks array
  let sum = marks.reduce(function(total, num)
  {return total + num}, 0)
  
  let average = sum / marks.length
  return Math.floor(average)
  }
  
  getAverage([1,2,3,4,5,]);
 
